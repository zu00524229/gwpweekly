name: 每週自動更新 AI 新聞

on:
  schedule:
    - cron: '0 0 * * 1'  # 每週一 00:00 UTC = 台灣時間 08:00
  workflow_dispatch:       # 也可以在 GitHub 手動觸發

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: 觸發新聞更新
        run: |
          curl -f https://gwpweekly.pages.dev/api/refresh
